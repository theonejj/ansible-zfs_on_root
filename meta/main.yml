---
galaxy_info:
  author: Richard Durso
  description: |
    ZFS on Root for Ubuntu 22.04 with rules based on device numbers presented.
    Native ZFS Encryption, UEFI, multiple non-root accounts, Dropbear support and some SSH tweaks.

  platforms:
    - name: Ubuntu
      release: jammy
#      versions:
#        - 22.04

  license: "license (BSD, MIT)"

  min_ansible_version: "2.4"

  galaxy_tags:
    - zfs
    - ubuntu
    - uefi
    - nonroot
    - dropbear
    - baremetal

dependencies:
  # Ansible role deploys msmtp as a mailer for Debian, Ubuntu
  - role: chriswayg.msmtp-mailer
